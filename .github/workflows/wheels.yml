# Build the platform wheels, and publish them to PyPI on a version tag.
#
# The same job builds on every push and pull request, so the release path
# is the path that has already been exercised hundreds of times by the
# time a tag reaches it. Only the publish job is gated on the tag.
#
# Not cibuildwheel: it exists to build one wheel per CPython version, and
# stanli ships a ctypes-loaded shared library with no extension module, so
# there is exactly one py3-none-<platform> wheel per platform.
#
# Windows is not here yet. It needs three things this workflow does not
# have: opam 2.2+ native Windows support to build the stanc3 object, a
# mingw-w64 toolchain (stan-math does not build under MSVC, which is why
# RStan ships through RTools), and a version-script equivalent for the
# export list. Track it separately rather than shipping a wheel nobody
# has run.
name: wheels

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  # Must match tools/dev_setup.sh. The embedded compiler cache is keyed on
  # it, so bumping it there and here invalidates the cache exactly once.
  STANC3_SRC_SHA: ac69570adecc41925b4ad72b6b2681c98c09c57d
  OCAML_VERSION: 5.5.0

jobs:
  build:
    name: ${{ matrix.plat }}
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            plat: macosx_11_0_arm64
          - os: macos-13
            plat: macosx_10_15_x86_64
          - os: ubuntu-24.04
            plat: manylinux_2_28_x86_64
            container: quay.io/pypa/manylinux_2_28_x86_64
          - os: ubuntu-24.04-arm
            plat: manylinux_2_28_aarch64
            container: quay.io/pypa/manylinux_2_28_aarch64
    steps:
      - uses: actions/checkout@v4

      # 11 MB object, ~30 minutes to build. Keyed on the stanc3 commit and
      # the OCaml version, so a hit skips the entire opam path below.
      - name: Restore the embedded stanc3 object
        id: stanc-cache
        uses: actions/cache@v4
        with:
          path: deps/stanc3/stanc_embed.o
          key: stanc-embed-${{ matrix.plat }}-${{ env.STANC3_SRC_SHA }}-ocaml${{ env.OCAML_VERSION }}

      - name: System packages (manylinux)
        if: matrix.container != ''
        run: |
          dnf install -y bubblewrap unzip which diffutils patch
          curl -fsSL https://opam.ocaml.org/install.sh | sh -s /usr/local/bin

      - name: System packages (macOS)
        if: matrix.container == ''
        run: brew install opam cmake

      - name: Vendored dependencies
        run: ./deps/fetch.sh

      - name: Build the embedded stanc3 object
        if: steps.stanc-cache.outputs.cache-hit != 'true'
        run: |
          opam init --bare --disable-sandboxing --no-setup -y
          ./tools/dev_setup.sh --embed
        env:
          OPAMYES: '1'

      - name: Configure and build
        run: |
          cmake -B build-rel -DCMAKE_BUILD_TYPE=Release \
            -DSTANLI_STANC_EMBED_OBJ="$PWD/deps/stanc3/stanc_embed.o" \
            -DSTANLI_OCAML_STDLIB="$(opam var lib --switch=stanc3-55)/ocaml"
          cmake --build build-rel -j

      - name: Tests
        run: ctest --test-dir build-rel --output-on-failure

      - name: Wheel
        run: ./tools/build_wheel.sh

      # The library is the whole wheel, so proving it loads and samples is
      # the only smoke test worth running here.
      - name: Install the wheel and sample
        run: |
          python3 -m venv /tmp/wheeltest
          /tmp/wheeltest/bin/pip install dist/*.whl
          /tmp/wheeltest/bin/python -c "
          import stanli
          m = stanli.Model(stan_file='tests/fixtures/es.stan',
                           data='tests/fixtures/eight_schools.json')
          d = m.sample(seed=1, warmup=1000, samples=1000)
          mu = d['mu'].mean()
          assert 3.0 < mu < 6.0, mu
          print('mu', mu)
          "

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.plat }}
          path: dist/*.whl

  publish:
    # Tags only. PyPI versions can never be reused, so this must not fire
    # on a branch push.
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write  # PyPI trusted publishing; no API token in secrets
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: wheel-*
          merge-multiple: true

      # setup.py holds the version; the tag has to agree with it, or a
      # mistyped tag silently publishes the wrong number.
      - name: Tag matches the packaged version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          pkg=$(python3 -c "import re,pathlib;
          print(re.search(r'version=\"([^\"]+)\"',
                pathlib.Path('python/setup.py').read_text()).group(1))")
          echo "tag $tag, setup.py $pkg"
          [ "$tag" = "$pkg" ]

      - uses: pypa/gh-action-pypi-publish@release/v1
