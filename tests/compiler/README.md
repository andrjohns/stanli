# Compiler producer fixtures

These models exercise the native-OCaml/js_of_ocaml byte contract in
`tests/test_portable_stancjs.cjs`. They are compiler transport fixtures, not
CmdStan numerical-reference models; language fixtures with recorded log-density
and gradient references belong in `tests/stanc3/` instead.
